import { Button } from "@/components/elements";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "All Playlists",
};

export default function Page() {
  // Return JSX
  return <Button>All Playlists</Button>;
}
